trigger_jenkins:
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'  # Только для пуша в main
  script:
    - |
      echo "🚀 Триггерим Jenkins для коммита: $CI_COMMIT_SHA"
      curl -X POST "${JENKINS_URL}/job/clone-and-deploy/buildWithParameters" \
        --data "GITLAB_REPO_URL=${CI_REPOSITORY_URL}" \
        --data "COMMIT_SHA=${CI_COMMIT_SHA}"
